name: Keep Render Warm
on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl health endpoint
        run: curl -sS -m 20 https://aehsas-chatbot.onrender.com/health || true
